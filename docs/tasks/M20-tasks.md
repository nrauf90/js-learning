# M20 — Multiple accounts/wallets + transfers tasks

Depends on M19 complete.

- [ ] **M20-T1** Migration + `Account` model; nullable `account_id` on `cash_entries`
- [ ] **M20-T2** `AccountController` CRUD + balance calculation
- [ ] **M20-T3** Transfer support between accounts without double-counting in income/expense reports
- [ ] **M20-T4** Frontend: account selector on entry form, accounts management page, balance widgets
- [ ] **M20-T5** Tests + `npm run qa:milestone -- M20`

## Completion log

_(populated as each task is finished)_
