# M15 — UX & accessibility polish tasks

Depends on M14 complete.

- [ ] **M15-T1** Focus trap + `role="dialog"`/`aria-modal` + return-focus for admin modals and the mobile sidebar drawer
- [ ] **M15-T2** Replace cashflow's `confirm()` delete with the app's own modal pattern
- [ ] **M15-T3** Fix cashflow empty-state flash (visible before first fetch resolves) and dashboard's duplicate empty-state-plus-zero-rows bug
- [ ] **M15-T4** `js/nav.js`: hide the Cash Flow link for logged-out users
- [ ] **M15-T5** Standardize currency formatting on `js/number-format.js` and date formatting/button classes across dashboard/cashflow/reports
- [ ] **M15-T6** Contrast pass on `--text-muted`; add a skip link; visible focus ring on `.sidebar-toggle`
- [ ] **M15-T7** Onboarding CTA on cashflow's empty state ("add your first entry")
- [ ] **M15-T8** Tests + `npm run qa:milestone -- M15`

## Completion log

_(populated as each task is finished)_
