# M30 — Multi-currency support tasks

Depends on M29 complete.

- [ ] **M30-T1** `currency` field on `cash_entries` (default `PKR`) + user default currency preference
- [ ] **M30-T2** FX conversion for display/report totals (static/admin-managed rate table)
- [ ] **M30-T3** Frontend: currency selector on entry form; converted totals alongside original amounts
- [ ] **M30-T4** Tests + `npm run qa:milestone -- M30`

## Completion log

_(populated as each task is finished)_
