# M17 — Budgets & spending limits tasks

Depends on M16 complete.

- [ ] **M17-T1** Migration + `Budget` model (`user_id`, `category_id`, `month`, `limit_amount`)
- [ ] **M17-T2** `BudgetController` CRUD API, scoped to the authenticated user
- [ ] **M17-T3** Frontend: set/edit budgets per category
- [ ] **M17-T4** Frontend: progress bar + over-budget indicator on dashboard/cashflow
- [ ] **M17-T5** Tests + `npm run qa:milestone -- M17`

## Completion log

_(populated as each task is finished)_
